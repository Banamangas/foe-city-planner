[project]
name = "foeopt"
version = "0.1.0"
description = "Forge of Empires city layout optimizer"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
